# razo-suite

One run of `packages/razo`'s own Playwright suite (45 tests) at the commit
named in `run.json`, captured with `scripts/capture-run.mjs`. Contains the
suite's one deliberate failure (the demo export without quality), steps
that healed through the implicit role fallback, and a failed step with
DOM candidates. Not synthetic.
